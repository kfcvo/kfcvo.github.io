# Chen Zhiyu Portfolio

Personal portfolio for product management, AI exploration, and visual practice.

The site is statically exported and deployed to GitHub Pages. The existing
`game` directory is preserved as an independent page.

## Local development

```bash
npm install
npx next dev
```

## Production build

```bash
npx next build
```
